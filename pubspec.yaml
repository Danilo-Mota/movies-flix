name: jeraflix_danilo
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  
  #API
  dio: ^4.0.6
  pretty_dio_logger: ^1.1.1
  #UI
  flutter_svg: ^1.0.3
  google_fonts: ^4.0.1
  carousel_slider: ^4.1.1
  #Cache
  cached_network_image: ^3.2.3
  #Formatter
  intl: ^0.18.0

  go_router: ^7.1.1
  get_it: ^7.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  generate: true
  assets:
    - assets/images/